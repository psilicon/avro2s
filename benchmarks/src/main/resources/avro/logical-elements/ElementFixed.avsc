{
  "type": "record",
  "name": "ElementFixed",
  "namespace": "avro2s.benchmarks.models",
  "doc": "One logical type per record, held in a collection, so the measurement is dominated by the per-element cost of converting it rather than by anything else in the record. control: a fixed with no logical type, the baseline for decimal-on-fixed and duration",
  "fields": [
    {
      "name": "values",
      "type": {
        "type": "array",
        "items": {
          "type": "fixed",
          "name": "PlainFixed16",
          "namespace": "avro2s.benchmarks.models",
          "size": 16
        }
      }
    }
  ]
}
